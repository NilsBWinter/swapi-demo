{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "push-dir --dir=dist/_nuxt --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.6",
    "push-dir": "^0.4.1",
    "sass": "^1.54.4",
    "sass-loader": "10.1.1"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.3.1",
    "axios": "^0.27.2"
  }
}
